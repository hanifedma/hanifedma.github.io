# Heavy source assets kept locally — the committed page uses the
# compressed/optimized versions (corrosion_demo.mp4, iotm.webp, iotm.jpg).
portfolio/Video DoaIbu_v4.mp4
portfolio/iotm.png
portfolio/f1tenth_car.png
portfolio/awarding.jpeg

# Raw source for the 6-DOF project — kept local only. The site uses the
# optimized robot6dof.jpg / robot6dof_demo.webm, and the code has its own repo
# (github.com/hanifedma/robot-6dof-opengl). The folder also holds a personal
# academic PDF (with a student ID) that must NOT be published to the public site.
portfolio/robot_6dof_opengl/

# LaTeX portfolio and resume sources — kept local only, nothing on the site
# links into these folders (the published resume is the root-level
# Resume_Hanif_Edma_Fauz.pdf).
portfolio-pdf/
resume/
